//sort() = method used to sort elements of an array in place.
//         sort elements as strings in lexicographical order ,not alphabetical
//         lexicographic = (alphabets + numbers + symbols) as strings

// let fruits = ["apple","banana","orange","grapes","mango"];
// console.log(fruits);
// fruits.sort();
// console.log(fruits);

// let numbers = [1,2,3,5,22,1,31,31,43,12,11];   // take these numbers as strings
// console.log(numbers);
// numbers.sort((a,b)=>a-b);           // a,b are 2 continguus elements
// console.log(numbers);
// numbers.sort((a,b)=>b-a);  
// console.log(numbers);

//onst people = [{name:"Prasanna",age:19},{name:"joshi",age:8},{name:"Ishan",age:2},{name:"Pavan",age:21}];
// people.sort((a,b)=>a.age-b.age);
// console.log(people);

// people.sort((a,b)=>b.name-a.name);   // it will not give correct output
// console.log(people);
// people.sort((a,b)=>a.name.localeCompare(b.name));
// console.log(people);
// people.sort((a,b)=>b.name.localeCompare(a.name));
// console.log(people);

//which method we use at last that only will apply for all and if we print before that method then latest methos rules only apply for that 

